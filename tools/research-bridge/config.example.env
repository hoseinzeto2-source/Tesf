# Research Bridge — secrets (DO NOT COMMIT)
# Copy from config.example.env and fill in.
# After sharing a password in chat, CHANGE IT on the VPS.

VPS_HOST=185.92.182.88
VPS_PORT=22
VPS_USER=root
# Prefer SSH key auth. Leave empty if using key only.
VPS_PASSWORD=

# Ports on VPS that phone reverse-tunnels into
VPS_ADB_PORT=15555
VPS_TERMUX_SSH_PORT=18022
VPS_FRIDA_PORT=27042
